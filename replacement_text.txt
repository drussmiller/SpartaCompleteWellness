      // Use the fixed progression calculation based on team join date
      const { 
        daysSinceStart, 
        weekNumber, 
        dayNumber, 
        progressDays,
        progressStart,
        utcNow,
        userLocalNow,
        userStartOfDay 
      } = calculateUserProgression(user, toUserLocalTime);